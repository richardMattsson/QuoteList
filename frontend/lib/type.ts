export type QuoteType = {
  id: number;
  name: string;
  quote: string;
};

export type FormType = {
  name: string;
  quote: string;
};

export type FormButtonType = {
  add: boolean;
  update: boolean;
};
